services:
    redis:
        image: redis:latest
        container_name: refinery-redis
        ports:
            - '6379:6379'
        volumes:
            - redis-volume:/data
        restart: unless-stopped

volumes:
    redis-volume:
        external: true
        name: redis-volume